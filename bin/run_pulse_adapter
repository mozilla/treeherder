#!/bin/bash
  source /etc/profile.d/treeherder.sh

  if [ -f ../venv/bin/python ]
  then
    source ../venv/bin/activate
    PYTHON=../venv/bin/python
  else
    PYTHON=python
  fi

  exec $PYTHON manage.py start_pulse_consumer --start
