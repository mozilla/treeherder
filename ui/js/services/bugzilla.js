'use strict';

treeherder.factory('bugzilla', [
    '$http', '$q',
    function($http, $q) {
        var urlbase = 'https://bugzilla.mozilla.org/rest/bug/';

        return "hello";
    }]);

